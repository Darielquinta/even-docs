{
  "name": "even-g2-docs-keyboard",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "pack": "npm run build && evenhub pack app.json dist -o g2-docs-keyboard.ehpk"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.10",
    "vite": "^6.3.3"
  },
  "devDependencies": {
    "@evenrealities/evenhub-cli": "^0.1.13",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": "^20.0.0 || >=22.0.0"
  }
}
